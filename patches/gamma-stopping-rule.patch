diff --git a/algorithms/bench/neighborsTime.C b/algorithms/bench/neighborsTime.C
index 8a6346b..3b17ad7 100644
--- a/algorithms/bench/neighborsTime.C
+++ b/algorithms/bench/neighborsTime.C
@@ -98,6 +98,14 @@ int main(int argc, char* argv[]) {
   long k = P.getOptionIntValue("-k", 0);
   if (k > 1000 || k < 0) P.badArgument();
   double alpha = P.getOptionDoubleValue("-alpha", 1.0);
+  // gamma stopping rule: -gamma in (0,1] replaces the max-out-degree stop.
+  // -S is the sample size; omit it for the default S = 10 ln n.
+  double gamma = P.getOptionDoubleValue("-gamma", 0.0);
+  long sample_size = P.getOptionIntValue("-S", 0);
+  if (gamma < 0.0 || gamma > 1.0) {
+    std::cout << "-gamma must be in (0, 1], got " << gamma << std::endl;
+    abort();
+  }
   int num_passes = P.getOptionIntValue("-num_passes", 1);
   int two_pass = P.getOptionIntValue("-two_pass", 0);
   if(two_pass > 1 | two_pass < 0) P.badArgument();
@@ -153,6 +161,8 @@ int main(int argc, char* argv[]) {
                                is_early_stop, esr,
                                rtype, radius, graph_stats,
                                prefix_size);
+  BP.gamma = gamma;
+  BP.sample_size = sample_size;
   long maxDeg = BP.max_degree();
 
   if((tp != "uint8") && (tp != "int8") && (tp != "float")){
diff --git a/algorithms/utils/types.h b/algorithms/utils/types.h
index 4c60918..509ba0d 100644
--- a/algorithms/utils/types.h
+++ b/algorithms/utils/types.h
@@ -180,6 +180,14 @@ struct BuildParams{
   double radius; // for radius search
   int prefix_size = 0;
 
+  // --- gamma stopping rule (this project's Vamana variant) ----------------
+  // gamma > 0 switches robustPrune from "stop at R edges" to "stop once the
+  // out-neighbourhood alpha-covers a gamma fraction of a uniform sample".
+  // sample_size <= 0 means the default S = 10 ln n. Both are defaulted, so
+  // every existing BuildParams construction is unaffected.
+  double gamma = 0.0;
+  long sample_size = 0;
+
   std::string alg_type;
 
   BuildParams(long R, long L, double a, int num_passes,
